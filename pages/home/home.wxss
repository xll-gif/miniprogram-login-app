/* home.wxss */
.container {
  display: flex;
  flex-direction: column;
  align-items: center;
  min-height: 100vh;
  padding: 40rpx 48rpx;
  background-color: #ffffff;
}

/* 成功图标 */
.success-icon {
  width: 160rpx;
  height: 160rpx;
  margin-top: 160rpx;
  margin-bottom: 48rpx;
}

/* 标题 */
.title {
  font-size: 48rpx;
  font-weight: bold;
  color: #000000;
  margin-bottom: 32rpx;
}

/* 副标题 */
.subtitle {
  font-size: 32rpx;
  color: #999999;
  margin-bottom: 96rpx;
}

/* 退出按钮 */
.logout-btn {
  width: 100%;
  height: 100rpx;
  background-color: #e0e0e0;
  color: #000000;
  border: none;
  border-radius: 20rpx;
  font-size: 32rpx;
  font-weight: 500;
  line-height: 100rpx;
}

.logout-btn::after {
  border: none;
}

.btn-hover {
  opacity: 0.8;
}
